void main() {
  String store = "who are what";

  String? result;
    

  for (int i = 0; i < store.length; i++) {
  
    for (int k = i + 1; k < store.length; k++) {
      if (store[i] == store[k]) {
        
        
        result = store[i];
      
        break;
      } else {
        result = "no repeat";
      }
    }
    break;
  }

  print(result);
}
